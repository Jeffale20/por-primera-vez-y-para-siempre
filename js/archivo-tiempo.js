const botonAbrirNota = document.getElementById("abrirNota"); const notaOculta = document.getElementById("notaOculta"); const fechaHoy = document.getElementById("fechaHoy"); if (fechaHoy) { const fechaActual = new Date(); const fechaFormateada = new Intl.DateTimeFormat("es-CO", { day: "numeric", month: "long", year: "numeric" }).format(fechaActual); fechaHoy.textContent = fechaFormateada; } if (botonAbrirNota && notaOculta) { botonAbrirNota.addEventListener("click", () => { notaOculta.classList.add("esta-visible"); notaOculta.setAttribute("aria-hidden", "false"); setTimeout(() => { notaOculta.scrollIntoView({ behavior: "smooth", block: "start" }); }, 150); }); } const botonesPuerta = document.querySelectorAll(".puerta-tiempo"); const contenidosPuerta = document.querySelectorAll(".contenido-puerta"); botonesPuerta.forEach((boton) => { boton.addEventListener("click", () => { const puertaSeleccionada = boton.dataset.puerta; botonesPuerta.forEach((puerta) => { puerta.classList.remove("activa"); puerta.setAttribute("aria-expanded", "false"); }); contenidosPuerta.forEach((contenido) => { contenido.classList.remove("activo"); contenido.setAttribute("hidden", ""); }); boton.classList.add("activa"); boton.setAttribute("aria-expanded", "true"); const contenidoActivo = document.getElementById( `contenido-${puertaSeleccionada}` ); if (contenidoActivo) { contenidoActivo.classList.add("activo"); contenidoActivo.removeAttribute("hidden"); setTimeout(() => { contenidoActivo.scrollIntoView({ behavior: "smooth", block: "nearest" }); }, 100); } }); });